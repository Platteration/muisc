/**
 * Numeric quality metrics for rendered transitions (clicks, level jumps, true peak, seam identity, beat alignment,
 * golden fingerprints). Used verbatim by tests, the CLI and the in-app Transition Lab. Implemented in the metrics
 * work package.
 */
package dev.muisc.metrics

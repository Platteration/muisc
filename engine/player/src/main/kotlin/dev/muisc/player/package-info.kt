/**
 * Pure-JVM program player: walks a [dev.muisc.transitions.PlaybackProgram] sample-accurately (album gapless,
 * rendered transitions, live DJ moves) into an [AudioSink]. Shared by the CLI (WAV / javax.sound) and the Android
 * app (AudioTrack). Implemented in the player work package.
 */
package dev.muisc.player
